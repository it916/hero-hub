.claude/

# Node modules — solo se instalan localmente para tareas puntuales (ej.
# generar PNGs de íconos con sharp). El proyecto es HTML plano sin bundler.
node_modules/
package-lock.json

# Datos financieros reales — usados solo para migración local, no para el repo
INGRESOS 2026.csv
INGRESOS 2026.xlsx

# Fuente original de la guía de contracting (AGA) — ya se extrajo lo necesario
# a data/contracting.json y las imágenes a images/contracting/.
# El HTML pesa 1.3 MB y la carpeta de assets otros 7 MB con trackers de terceros.
Contracting Information A through L - Applied General Agency, LLC.html
Contracting Information A through L - Applied General Agency, LLC_files/
scratch_parse_contracting.js

# Directorio temporal para debugging local del parser de Excel de ingresos.
.scratch-migrate/

# Excel maestro de URLs de reportes de Gilbana — no va al repo.
URLS_INGRESOS_2026.xlsx

# Backups descargados desde el panel de migración de datos (contienen data
# personal del equipo — emails, teléfonos, bios). Se generan localmente cuando
# se corre el migrador y no aportan al código.
hero-hub-backup-*.json
